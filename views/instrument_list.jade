extends layout 

block content 
    .main-content
        h1=title

        ul  
            each instrument in instrument_list 
                li 
                    a(href=instrument.url) #{instrument.name} 
                    | (#{instrument.manufacturer.name})

            else 
                li There are no instruments. 

        p 
            a(href='/catalog/instrument/create') Create Instrument